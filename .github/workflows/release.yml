on:
  release:
    types: [created]
  workflow_dispatch:

permissions:
  contents: read

name: Release

jobs:
  # This job builds the self-contained executable for each supported platform
  build-artifacts:
    name: Build Binary
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            platform_name: linux
            arch_name: x64
            toolbox_os: linux
            toolbox_arch: amd64
            toolbox_ext: ""
          - os: macos-latest
            platform_name: darwin
            arch_name: arm64
            toolbox_os: darwin
            toolbox_arch: arm64
            toolbox_ext: ""
          - os: windows-latest
            platform_name: win32
            arch_name: x64
            toolbox_os: windows
            toolbox_arch: amd64
            toolbox_ext: ".exe"

    steps:
      # Checkout the repository code
      - uses: actions/checkout@v4

      # Install the `uv` Python package manager which handles fast installations
      - name: Install uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          cache-dependency-glob: "mcp/uv.lock"

      # Setup the correct Python version defined by uv
      - name: Set up Python
        run: uv python install

      # Install all project dependencies defined in pyproject.toml / uv.lock
      - name: Install dependencies
        working-directory: mcp
        run: uv sync --frozen

      # Extract the targeted genai-toolbox version from pyproject.toml
      - name: Get Toolbox Version
        working-directory: mcp
        shell: bash
        run: |
          TOOLBOX_VERSION=$(grep -o '^toolbox_version = "[^"]*"' pyproject.toml | cut -d '"' -f 2)
          echo "Found toolbox version: ${TOOLBOX_VERSION}"
          echo "TOOLBOX_VERSION=${TOOLBOX_VERSION}" >> $GITHUB_ENV

      # Download the pre-compiled genai-toolbox binary for the target platform
      - name: Download genai-toolbox binary
        working-directory: mcp
        shell: bash
        run: |
          DOWNLOAD_URL="https://storage.googleapis.com/genai-toolbox/v${{ env.TOOLBOX_VERSION }}/${{ matrix.toolbox_os }}/${{ matrix.toolbox_arch }}/toolbox${{ matrix.toolbox_ext }}"
          echo "Downloading toolbox from: ${DOWNLOAD_URL}"
          curl -L --fail -o "toolbox${{ matrix.toolbox_ext }}" "${DOWNLOAD_URL}"
          chmod +x "toolbox${{ matrix.toolbox_ext }}"

      # Package the Python application into a standalone executable using PyInstaller
      - name: Build binary with PyInstaller
        working-directory: mcp
        run: uv run pyinstaller pyinstaller.spec

      # Prepare the final distribution folder containing the binary, config, and docs
      - name: Prepare distribution
        working-directory: mcp
        shell: bash
        run: |
          mkdir -p staging
          
          if [ "${{ runner.os }}" == "Windows" ]; then
            mv dist/db-context-enrichment.exe staging/
            mv toolbox.exe staging/
            BINARY_NAME="\${extensionPath}/db-context-enrichment.exe"
            TOOLBOX_NAME="\${extensionPath}/toolbox.exe"
          else
            mv dist/db-context-enrichment staging/
            mv toolbox staging/
            BINARY_NAME="\${extensionPath}/db-context-enrichment"
            TOOLBOX_NAME="\${extensionPath}/toolbox"
          fi
          
          jq ".contextFileName = \"GEMINI.md\" | .mcpServers.mcp_db_context_enrichment.command = \"$BINARY_NAME\" | .mcpServers.mcp_db_context_enrichment.args = [] | .mcpServers.mcp_toolbox = {\"command\": \"$TOOLBOX_NAME\", \"args\": [\"--stdio\"]}" ../gemini-extension.json > staging/gemini-extension.json
          cp GEMINI.md staging/
          cp ../LICENSE staging/

      # Create a compressed tarball archive for Linux and macOS distributions
      - name: Archive artifacts (Unix)
        if: runner.os != 'Windows'
        working-directory: mcp/staging
        run: |
          tar -czf ../../${{ matrix.platform_name }}.${{ matrix.arch_name }}.db-context-enrichment.tar.gz *

      # Create a zip archive for Windows distributions
      - name: Archive artifacts (Windows)
        if: runner.os == 'Windows'
        working-directory: mcp/staging
        run: |
          Compress-Archive -Path * -DestinationPath ../../${{ matrix.platform_name }}.${{ matrix.arch_name }}.db-context-enrichment.zip

      # Upload the archives to GitHub Actions artifacts so the publish job can access them
      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binary-${{ matrix.platform_name }}-${{ matrix.arch_name }}
          path: |
            ${{ matrix.platform_name }}.${{ matrix.arch_name }}.db-context-enrichment.tar.gz
            ${{ matrix.platform_name }}.${{ matrix.arch_name }}.db-context-enrichment.zip
          if-no-files-found: error
          retention-days: 1

  # This job gathers the built executables and attaches them as release assets
  publish-release:
    needs: build-artifacts
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      # Download all platform archives generated by the build-artifacts job
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-assets
          pattern: binary-*
          merge-multiple: true

      # Attach the downloaded archives as assets to the created GitHub Release
      - name: Upload Release Assets
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event.release.tag_name }}
          files: release-assets/*
